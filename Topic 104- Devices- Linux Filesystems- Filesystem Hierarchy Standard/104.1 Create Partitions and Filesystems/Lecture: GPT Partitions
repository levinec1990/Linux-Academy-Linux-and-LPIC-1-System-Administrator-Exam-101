*GPT Partition Tools

$ gdisk
This is a command in the style of 'fdisk' that can be used to create GPT partitions on disks.

$ parted
The parted command can be used to create both MBR and GPT disk partitions. 

