*Text Manipulation

$ sed
The 'stream editor' command. Used for a variety of task, commonly utilized to alter text in a file or search and replace words. 

$ split
Command that is used to split a file up into individual pieces. By default, each piece of the file contains up to 1000 lines but this can be changed to a different value or file size. 

